import setuptools

setuptools.setup(
    name="AppShared",
    version="1.0",
    description="Shared utils for App services",
    author="badgerdev",
    author_email="ed@localhost.com",
    packages=[
        "AppShared"
    ],
    install_requires=[
    ],  # external packages as dependencies
)